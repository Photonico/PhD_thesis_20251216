> 后续更新：2026-09-24 的原数据重画与交叉核对发现了进一步修正，尤其涉及 PDoS、EPW 能隙文件及 Tc 解释。请同时阅读 [本轮记录](ai_proj12_figure_style.md)；本文件保留为上一轮审阅记录。

# Project 1 最小修改记录

日期：2026-09-23。对应 `src/project_1_main.tex` 与 `src/project_1_SI.tex`；本章在当前 thesis 中为 Chapter 5。

## 原则与评审对应

保留已发表论文的结构、原始计算、能带、结构参数和主要比较。参考 `ai_proj1_first.md`，但逐项核对 examiner 原文、作者贡献记录、原图和归档数据，不照搬其中的英文模板或计算参数。

Examiner 1 要求明确个人贡献，并使原 Figures 4.29–4.36 的图注自包含；Examiner 2 同时指出部分图中文字印刷后过小。对应当前 labels 为 `S1.17`–`S1.24`。本次在正文补足发表与真实分工，八张附图同步改图与图注。Examiner 2 的可读性要求覆盖本章所有图，原 Figures 4.29、4.30 只是举例，因此对全章 49 个不同图件均作了核查。引言用一小段明确本章贡献为结构与性质之间的比较，保留原来的研究动机。

## 已应用的正文修改

1. 章首注明 Nanomaterials 14, 1659 (2024)，按原论文 Author Contributions 与 `.agents/proj_contributors.md` 陈述分工。没有把构思、初稿等工作全部归到 candidate 一人。
2. 保留作者原有段落、句式与公式习惯。修复三处 `\vec{r},)` 为 `\vec{r}\,)`；没有给仅出现在下标中的矢量追加薄空格。
3. 明确 `\omega` 是角频率，横轴为光子能量 `\hbar\omega`。吸收起点不再等同于介电虚部第一峰。
4. 将 HSE06 的优势写为相对于 PBE 的可能改善，不声称它保证正确实验带隙。
5. 区分负实部、损失峰与低阻尼 plasmon 判据；现有谱线不足以给出二维 plasmon 色散和寿命。
6. 保留原来显示的完整介电张量，但撤回由反对称非对角项直接推出自然旋光的论证。对于局域、互易的响应，三重旋转与对称张量并不允许原文所称的那些非对角项。P3 结构事实、原始曲线和原有能量范围均保留，张量对称性作为需源头核查的问题明确交代。
7. 标量光学公式只用于对角主轴近似；不把每个非对角矩阵元分别解释为吸收系数、折射率或反射率。对于尚未完成张量核查的体系，对角派生谱的解释也保留此条件。

## 每张图的改动

- `S1.17_alt.pdf`：Graphene–Borophene 的 3×3 张量；保留全部原曲线，放大字号，共用原生图例。PBE 绿色、HSE06 橙色；实部实线、虚部虚线。
- `S1.18.pdf`：Graphene–BC3 的 3×3 张量；同样只改布局与字号。PBE 蓝色、HSE06 橙色。
- `S1.19.pdf`：Graphene–B4C3 的 3×3 张量；同样只改布局与字号。PBE 紫色、HSE06 橙色。
- `S1.20.pdf`：HSE06 吸收系数，保留 xx、yy、zz 三个对角面板；修正角频率换算，标明 nm^-1。
- `S1.21.pdf`：HSE06 能量损失函数，保留 xx、yy、zz；对角曲线数值不变。
- `S1.22.pdf`：HSE06 折射率，保留 xx、yy、zz；对角曲线数值不变。
- `S1.23_correct.pdf`：HSE06 反射率，保留 xx、yy、zz；对角曲线数值不变。
- `S1.24_correct.pdf`：HSE06 消光系数，保留 xx、yy、zz；对角曲线数值不变。
- `proj1.12a.pdf`：正文两种偏振的吸收系数，同步修正角频率换算，保留原材料、峰位与相对大小。
- `S1.14a.pdf`：四种单层 PBE 吸收系数，同步修正角频率换算；原图误写 bilayers 的标题改为 monolayers。

后三种吸收图不能仅称为美化：旧程序将 E/h 代入要求 E/ℏ 的公式，使以 nm^-1 表示的吸收系数少了 2π。本次直接从原始介电数据重新求值，数值变为旧值的 2π 倍；峰位、介电数据与 DFT 计算均未改变。

补充谱线核对：BC3/B4C3 的尖锐面内损失峰限定为低于 1 eV，与 Borophene 约 2/5 eV 的实介电函数负区区分；面外主损失峰的概述改为约 17–19 eV。Borophene 的较强吸收限定到低能起始附近，不再声称全谱最强；BC3 实介电函数存在小负区，撤回全能区均为正的绝对表述。曲线数值未因此更改，详细数据见 `proj1_spectral_checks.json`。

## 全章图件可读性补查

除上述 10 个图件外，进一步完成以下必要排版修改；原始计算结果不变：

- `proj1.3a`–`proj1.3d`：四种单层能带，保留两种泛函的所有能带，放大字号和 Fermi 标识。
- `proj1.5`–`proj1.7`：三种双层的能带与 DoS，重新分配能带/DoS 宽度，共用图例放在图外。
- `proj1.8a`、`proj1.8b`：Schottky 示意图与能带例图改为上下排列；保留势垒参考线，修复原纵轴标题裁切；Ohmic Barrier 规范为 Ohmic contact。
- `proj1.9`–`proj1.11_diff`：正文介电谱放大字号；后两图保留实/虚两行、xx/zz/xy 三列及原 xy 能量范围。
- `proj1.12b`、`proj1.13a_cor`、`proj1.13b`、`proj1.14_cor`：正文 loss、R、n、extinction 图放大字号，保留原光谱和图例颜色。
- `S1.1`–`S1.3`：k 网格和结构优化能量图；S1.3 横排三面板改为 2×2 布局中三个面板，共用堆叠位置图例。
- `S1.4`–`S1.6`：原 PDF 文件不改。TeX 以无损裁框逐面板放大，用 ContinuedFloat 保持原图号；保留原热图、极值点、色条和刻度，不重新拟合能量面。
- `S1.9`–`S1.11`：PDoS 重新排列，保留各原子/轨道曲线和 Fermi 能量，用共用图例减少拥挤。
- `S1.12_alt`、`S1.15`、`S1.16_alt`：保留全部收敛测试参数及原曲线，放大坐标/标签，合并原生图例。
- `S1.13a`–`S1.13d`：四种单层的介电谱字号放大，整体底部说明实/虚线型。
- `S1.14b`、`S1.14c`、`S1.14e_correct`：loss、n、extinction 字号放大；五行组合图只在底部保留一份材料图例。
- `S1.14d_correct`：除放大字号外，还修正了 TeX 的旧文件误引用。原 `S1.14d.pdf` 的 12 条曲线不匹配同源介电数组的标准反射率公式，而仓库原有 `_correct` 版本的 12 条全部匹配；故改用后者。旧 `S1.14d.pdf` 文件没有覆盖。

三张原子结构/电荷图 `proj1.1`、`proj1.3`、`proj1.4` 的原标注已栅格化，打印后部分小字仍过小，因此保留原图像像素与面板内容，在图像外增加清晰的原生材料/视角标题；未覆盖原胞、长度标记、原子或电荷云。原小标签仍作为图像背景保留，避免改动结构信息。原嵌入图像无损提取到 `structure_sources/`，简洁入口为 `reproduce_structure_labels.py`。全章对可提取文字的图件复测最终 PDF 内的真实缩放，栅格化标注另作视觉核验。

因此，修改 PDF 文件较多不等于重新计算了这些结果。除三个吸收图的单位/角频率更正、单层 R 的正确文件引用，以及移除无独立标量可观测意义的非对角派生面板外，其余是字号和布局调整。

## 数据依据与复现

原始来源为 [Graphene-BC_20230622](https://github.com/Photonico/Graphene-BC_20230622)。10 份原始 `vaspout.h5` 的路径、固定 Git blob、SHA256、旧图曲线核对与新图核对均保存在 `figures_proj1/optics_sources.json`，避免仅凭当前可变化的 main 分支判断。

光学图原参考曲线与新 PDF 各有 291 条已逐条匹配；误引用的旧 R 图另保留 12 条失败匹配的证据。独立脚本为 `figures_proj1/regenerate_optics.py`，数据为 `figures_proj1/optics_data.npz`。无需安装或导入 vmatplot，不涉及插值、拟合或重新计算电子结构。

其余图件由 `reproduce_electronic_layout.py`、`reproduce_schottky.py` 和 `reproduce_readability.py` 重绘，各配一个 NPZ。数组保存原 PDF 的精确矢量顶点或 marker 中心及轴刻度的仿射映射，不是光栅描点；元数据记录原 Git blob、原路径序号和误差。这些是原图显示路径，不能冒称为已恢复完整 DFT 原始数据。

八张附图原有 PBE/HSE、颜色与分量并非 Gemini 稿所写的统一蓝橙比较；派生光学图实际仅有 HSE06，图注已据实修正。

## 保留的边界

非对角介电分量的源头与互易性仍需核查，不能声称本轮已验证旋光。二维有效介电量也不能不加条件地当作实验自由悬空单层的绝对光学常数。未为回应评审而新增 GW/BSE、磁态或载流子模型，亦未猜测未归档的参数。

补充源头核查：反号趋势已存在于 VASP 6.3.2 的原始 HDF5；两个 HSE 体系的 XML 全部 4000 点及 OUTCAR 六分量对应 HDF5 的下三角，未发现本次读取或复数拆分制造负号。分量也并非严格反对称，正文已改为近似反号。官方 py4vasp 作图会取转置分量平均，但原 OUTCAR 并不输出该平均值，不能以此声称异常已解决。两个 HSE 输出仅识别一个空间群操作，理想 P3 与实际数值结构仍需区分。定量结果和官方实现链接见 `proj1_tensor_evidence.json`。

## 编译核验

已使用 latexmk 完整编译 `.output/thesis.pdf`，最终共 330 页。Project 1 为印刷页 151–196，由原来的 36 页变为 46 页；增加主要来自按 Examiner 2 要求放大和拆分全章图件，另有章首贡献与完整图注。未新增研究小节或第一性原理计算。

本章关键图的最终印刷页：Figure 5.12 在 171；Figure 5.26 在 188；Figures 5.29–5.31 在 191–193；Figures 5.32–5.36 在 194–196。三个完整张量图的数字刻度约 8.27 pt，图例约 9.02 pt，原来缩印约 2.6–3.4 pt 的问题已修正。

已渲染检查章首、结构图、能带/DoS/PDoS、能量面裁图及续图、收敛图、吸收图、四行/五行组合图及八张目标附图。没有超页浮动体、未解析引用或新增 overfull box；原有九个 hbox 警告与修改前相同（其中三个来自基础章节，其余来自 Project 3 标题和文献表）。16 个章级目录目标仍全部位于奇数页。机器可核查记录为 `proj12_build_verification.json`。全章 49 个不同图件、57 次实际嵌入已逐一核对，无漏图；46 个含原生文字的图件，其常规坐标/标签最小印刷字号约 7.89 pt。其余 3 张能量面图的 11 个面板已经双人视觉复核。详细字号和页码见 `proj1_figure_readability.json`，矢量与像素一致性见 `proj1_layout_verification.json`。

发表列表及 Appendix 的章节号改为交叉引用，避免新增基础章后继续显示旧的 Chapter 4/5/6。
